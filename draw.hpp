#ifndef Draw_hpp
#define Process_hpp

#include <vector>
#include <iostream>
#include <SDL2/SDL.h>

const int WIDTH = 320;
const int HEIGHT = 64;

void draw(SDL_Surface *surface);

#endif /* Draw_hpp */
